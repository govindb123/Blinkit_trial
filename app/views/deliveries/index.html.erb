<div class="deliveries-container">
  <div class="deliveries-card">
    <div class="dashboard-header">
      <h2 class="deliveries-title">🚴 Rider Dashboard</h2>
      <%= link_to "🏠 Home", root_path, class: "home-btn" %>
    </div>
    
    <!-- Three Column Layout -->
    <div class="deliveries-columns">
      
      <!-- Column 1: Available Orders -->
      <div class="deliveries-column">
        <h3 class="column-title available-title">📦 Available Orders</h3>
        <% if @available_orders.any? %>
          <% @available_orders.each do |order| %>
            <div class="order-card available-card">
              <div class="new-badge">NEW</div>
              <div class="order-id">
                <%= link_to "Order ##{order.id}", delivery_path(order), class: "order-id" %>
              </div>
              <div class="order-address">📍 <%= order.address %></div>
              <div class="order-item available-item">🛍️ <%= order.order_items.first&.item&.name %> - <span class="item-price">₹<%= order.order_items.first&.item&.price %></span></div>
              <%= form_with url: assign_delivery_path(order), method: :patch, local: true do |f| %>
                <%= f.submit "🚀 Assign", class: "btn-assign" %>
              <% end %>
            </div>
          <% end %>
        <% else %>
          <div class="empty-state empty-available">
            <div class="empty-icon">📭</div>
            <p class="empty-text">No available orders</p>
          </div>
        <% end %>
      </div>

      <!-- Column 2: My Assigned Orders -->
      <div class="deliveries-column">
        <h3 class="column-title assigned-title">🚚 My Assigned Orders</h3>
        <% if @my_assigned_orders.any? %>
          <% @my_assigned_orders.each do |order| %>
            <div class="order-card assigned-card">
              <div class="status-badge <%= order.status == 'assigned' ? 'assigned-badge' : 'delivery-badge' %>"><%= order.status == 'assigned' ? '📋 ASSIGNED' : '🚚 OUT FOR DELIVERY' %></div>
              <div class="order-content">
                <div class="order-id">
                  <%= link_to "Order ##{order.id}", delivery_path(order), class: "order-id" %>
                </div>
                <div class="order-address">📍 <%= order.address %></div>
                <div class="order-item assigned-item">🛍️ <%= order.order_items.first&.item&.name %> - <span class="item-price">₹<%= order.order_items.first&.item&.price %></span></div>
                <% if order.status == "assigned" %>
                  <%= button_to "🚀 Start", start_delivery_delivery_path(order), method: :patch, class: "btn-start" %>
                <% elsif order.status == "out_for_delivery" %>
                  <%= button_to "✅ Deliver", delivery_path(order), method: :put, class: "btn-deliver" %>
                <% end %>
              </div>
            </div>
          <% end %>
        <% else %>
          <div class="empty-state empty-assigned">
            <div class="empty-icon">📋</div>
            <p class="empty-text">No assigned orders</p>
          </div>
        <% end %>
      </div>

      <!-- Column 3: Completed Deliveries -->
      <div class="deliveries-column">
        <h3 class="column-title completed-title">✅ Completed Deliveries</h3>
        <% if @delivered_orders.any? %>
          <% @delivered_orders.each do |order| %>
            <div class="order-card completed-card">
              <div class="status-badge done-badge">✅ DONE</div>
              <div class="order-content">
                <div class="order-id">
                  <%= link_to "Order ##{order.id}", delivery_path(order), class: "order-id" %>
                </div>
                <div class="order-address">📍 <%= order.address %></div>
                <div class="order-item completed-item">🛍️ <%= order.order_items.first&.item&.name %> - <span class="item-price">₹<%= order.order_items.first&.item&.price %></span></div>
                <div class="completed-checkmark">✅</div>
              </div>
            </div>
          <% end %>
        <% else %>
          <div class="empty-state empty-completed">
            <div class="empty-icon">📦</div>
            <p class="empty-text">No deliveries yet</p>
          </div>
        <% end %>
      </div>
      
    </div>
  </div>
</div>