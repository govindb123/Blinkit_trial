<div class="order-details-container">
  <div class="order-details-card">
    <h2 class="order-details-title">Order Details</h2>

    <div class="order-info">
      <p><strong>Order #<%= @order.id %></strong></p>
      <p><strong>Status:</strong> <span class="<%= @order.status == 'pending' ? 'status-pending' : 'status-delivered' %>"><%= @order.status.capitalize %></span></p>
      <p><strong>Order Date:</strong> <%= @order.created_at.strftime("%B %d, %Y at %I:%M %p") %></p>
      <p><strong>Delivery Address:</strong> <%= @order.address %></p>
    </div>

    <h3 class="items-section-title">Items Ordered:</h3>
    <% @order_items.each do |order_item| %>
      <div class="order-item-detail">
        <strong><%= order_item.item.name %></strong> - <span class="order-item-price">₹<%= order_item.item.price %></span>
        <br><small>Quantity: <%= order_item.quantity %></small>
      </div>
    <% end %>

    <div>
      <%= link_to "Back to Orders", orders_path, class: "back-btn" %>
    </div>
  </div>
</div>