<div class="items-container">
  <div class="items-card">
    <div class="dashboard-header">
      <h2 class="items-title">Seller Dashboard</h2>
      <%= link_to "🏠 Home", root_path, class: "home-btn" %>
    </div>
    
    <% if notice %>
      <div class="success-notice">
        <%= notice %>
      </div>
    <% end %>
    
    <div style="margin-bottom: 30px;">
      <%= link_to "Add New Item", new_item_path, class: "add-item-btn" %>
    </div>

    <h3 class="section-title">Your Items:</h3>
    
    <% if @items.any? %>
      <div class="items-grid">
        <% colors = ['#ffebee', '#e8f5e8', '#e3f2fd', '#fff3e0', '#f3e5f5', '#e0f2f1'] %>
        <% @items.each_with_index do |item, index| %>
          <div class="item-card" style="background-color: <%= colors[index % colors.length] %>;">
            <div class="item-name"><%= item.name %></div>
            <div class="item-price">₹<%= item.price %></div>
          </div>
        <% end %>
      </div>
    <% else %>
      <div class="empty-items">
        <p>No items added yet. Click "Add New Item" to get started!</p>
      </div>
    <% end %>
  </div>
</div>